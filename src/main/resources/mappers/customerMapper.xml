<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.DCShop.join.dao.CustomerDAO">
	<!-- 회원가입 처리 -->
	<insert id="insertCustomer" parameterType="com.spring.DCShop.join.dto.CustomerDTO">
		INSERT INTO MVC_CUSTOMER_TBL (user_id, user_password, user_name, user_birthday, user_address, user_hp, user_email) 
		VALUES (#{user_id}, #{user_password}, #{user_name}, #{user_birthday}, #{user_address}, #{user_hp}, #{user_email})
	</insert>
</mapper>