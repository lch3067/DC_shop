<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	  
<mapper namespace="com.spring.DCShop.shop.dao.ProductDAO">  <!-- namespace="패키지명.인터페이스" -->

	<!-- 상품상세페이지 -->
	<select id="productDetail" parameterType="int" resultType="com.spring.DCShop.shop.dto.ProductDTO">
		SELECT * FROM product_tbl
 		 WHERE pd_id = #{pd_id} 
	</select>
</mapper> 